import { Volume2 } from 'lucide-react'
import { useDemoStore } from '../../store/useDemoStore'
import { voiceService } from '../../services/voiceService'
export function VoicePreferences({voices,onPreview}:{voices:SpeechSynthesisVoice[];onPreview:()=>void}){const preferences=useDemoStore(s=>s.preferences.assistant),update=useDemoStore(s=>s.updatePreferences);const set=(values:Partial<typeof preferences>)=>update('assistant',values);return <section className="voice-preferences"><div className="voice-preferences-head"><div><span>VOZ DO ASSISTENTE</span><h3>Preferências de voz</h3></div><button className="btn icon-btn" aria-label="Ouvir voz selecionada" onClick={onPreview}><Volume2 size={16}/></button></div><label><span>Tipo de voz</span><div className="voice-gender">{([['female','Feminina'],['male','Masculina'],['auto','Automática']] as const).map(([value,label])=><button className={preferences.voiceGender===value?'active':''} onClick={()=>set({voiceGender:value,voice:''})} key={value}>{label}</button>)}</div></label><label><span>Escolher voz</span><select className="input" value={preferences.voice} onChange={event=>set({voice:event.target.value})}><option value="">Melhor voz disponível</option>{voices.map(voice=><option value={voice.name} key={`${voice.name}-${voice.lang}`}>{voiceService.describe(voice)}</option>)}</select></label><div className="voice-ranges"><Range label="Velocidade" value={preferences.speechRate} min={.65} max={1.35} step={.05} onChange={speechRate=>set({speechRate})}/><Range label="Tom" value={preferences.pitch} min={.7} max={1.4} step={.05} onChange={pitch=>set({pitch})}/><Range label="Volume" value={preferences.volume} min={0} max={1} step={.05} onChange={volume=>set({volume})}/></div><Toggle label="Ler resposta em voz alta" checked={preferences.readAloud} onChange={readAloud=>set({readAloud})}/><Toggle label="Interromper fala anterior" checked={preferences.interruptOnSend} onChange={interruptOnSend=>set({interruptOnSend})}/><Toggle label="Enviar após reconhecimento" checked={preferences.autoSendVoice} onChange={autoSendVoice=>set({autoSendVoice})}/></section>}
function Range({label,value,min,max,step,onChange}:{label:string;value:number;min:number;max:number;step:number;onChange:(value:number)=>void}){return <label><span>{label}<b>{value.toFixed(2)}</b></span><input type="range" min={min} max={max} step={step} value={value} onChange={event=>onChange(Number(event.target.value))}/></label>}
function Toggle({label,checked,onChange}:{label:string;checked:boolean;onChange:(checked:boolean)=>void}){return <label className="voice-toggle"><span>{label}</span><input type="checkbox" checked={checked} onChange={event=>onChange(event.target.checked)}/><i/></label>}
