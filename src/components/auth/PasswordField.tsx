import { useId,useState } from 'react'
import { Eye,EyeOff,LockKeyhole } from 'lucide-react'
export function PasswordField({label,value,onChange,autoComplete='current-password',error}:{label:string;value:string;onChange:(value:string)=>void;autoComplete?:string;error?:string}){const [visible,setVisible]=useState(false),id=useId(),errorId=`${id}-error`;return <label className="auth-field" htmlFor={id}><span>{label}</span><div className={error?'field-control invalid':'field-control'}><LockKeyhole aria-hidden="true"/><input id={id} name={autoComplete==='current-password'?'password':'new-password'} value={value} onChange={e=>onChange(e.target.value)} type={visible?'text':'password'} autoComplete={autoComplete} aria-invalid={Boolean(error)} aria-describedby={error?errorId:undefined} required/><button type="button" onClick={()=>setVisible(v=>!v)} aria-label={visible?'Ocultar senha':'Mostrar senha'} aria-pressed={visible}>{visible?<EyeOff/>:<Eye/>}</button></div>{error&&<small id={errorId}>{error}</small>}</label>}
