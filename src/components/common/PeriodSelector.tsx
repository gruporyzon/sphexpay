import { CalendarDays,ChevronDown } from 'lucide-react'
import { periodOptions } from '../../services/analyticsService'
import { useDemoStore } from '../../store/useDemoStore'
export function PeriodSelector(){const period=useDemoStore(s=>s.period),setPeriod=useDemoStore(s=>s.setPeriod);return <div className="period-selector internal-filter-control"><label className="internal-filter-select"><CalendarDays className="internal-filter-icon" aria-hidden="true"/><select aria-label="Período" className="internal-filter-value dashboard-period" value={period.preset} onChange={e=>setPeriod({...period,preset:e.target.value as typeof period.preset})}>{periodOptions.map(([key,label])=><option key={key} value={key}>{label}</option>)}</select><ChevronDown className="internal-filter-chevron" aria-hidden="true"/></label>{period.preset==='custom'&&<div className="period-custom"><input aria-label="Data inicial" type="date" className="input" value={period.from||''} onChange={e=>setPeriod({...period,from:e.target.value})}/><input aria-label="Data final" type="date" className="input" value={period.to||''} onChange={e=>setPeriod({...period,to:e.target.value})}/></div>}</div>}
