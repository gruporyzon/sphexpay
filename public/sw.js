const CACHE='sphexpay-shell-v7',SHELL=['/','/index.html','/offline.html','/manifest.webmanifest','/branding/sphexpay-logo-96.png','/icons/sphexpay-app-192.png','/icons/sphexpay-app-512.png','/favicon/favicon-32.png'],delivered=new Map()
self.addEventListener('install',event=>event.waitUntil(caches.open(CACHE).then(cache=>cache.addAll(SHELL)).then(()=>self.skipWaiting())))
self.addEventListener('activate',event=>event.waitUntil(caches.keys().then(keys=>Promise.all(keys.filter(key=>key!==CACHE).map(key=>caches.delete(key)))).then(()=>self.clients.claim())))
self.addEventListener('fetch',event=>{if(event.request.method!=='GET')return;event.respondWith(fetch(event.request).then(response=>{if(response.ok&&new URL(event.request.url).origin===self.location.origin){const copy=response.clone();event.waitUntil(caches.open(CACHE).then(cache=>cache.put(event.request,copy)))}return response}).catch(()=>caches.match(event.request).then(cached=>cached||(event.request.mode==='navigate'?caches.match('/offline.html'):Response.error()))))})
const validPayload=value=>value&&typeof value==='object'&&typeof value.eventId==='string'&&value.eventId.length>0&&typeof value.title==='string'&&value.title.length>0&&typeof value.body==='string'&&typeof value.route==='string'
const showDevice=async payload=>{if(!validPayload(payload))return false;const now=Date.now();for(const [id,time]of delivered)if(now-time>86400000)delivered.delete(id);if(delivered.has(payload.eventId))return false;delivered.set(payload.eventId,now);await self.registration.showNotification(payload.title,{body:payload.body,icon:'/icons/sphexpay-app-192.png',badge:'/icons/sphexpay-app-192.png',tag:payload.eventId,renotify:false,silent:false,data:{eventId:payload.eventId,type:payload.type,route:payload.route}});return true}
self.addEventListener('message',event=>{if(event.data?.type==='SKIP_WAITING'){self.skipWaiting();return}if(event.data?.type==='SHOW_DEVICE_NOTIFICATION')event.waitUntil(showDevice(event.data.payload))})
self.addEventListener('push',event=>{let payload;try{payload=event.data?.json()}catch{return}event.waitUntil(showDevice({...payload,eventId:payload?.eventId||payload?.notificationId||payload?.tag,route:payload?.route||payload?.url||payload?.path||'/app/notificacoes'}))})
self.addEventListener('notificationclick',event=>{event.notification.close();const route=event.notification.data?.route||'/app';event.waitUntil(self.clients.matchAll({type:'window',includeUncontrolled:true}).then(async clients=>{const target=clients.find(client=>new URL(client.url).origin===self.location.origin);if(target){await target.focus();if('navigate'in target)await target.navigate(route);return}await self.clients.openWindow(route)}))})
self.addEventListener('notificationclose',event=>{const eventId=event.notification.data?.eventId;if(eventId)delivered.set(eventId,Date.now())})
